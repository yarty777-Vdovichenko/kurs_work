import { Box, Typography } from "@mui/material";

export default function Charts()
{

    return(
        <Box sx={{height:"calc(100vh - 50px)",width:"100%",backgroundColor:"#0B2E1C",alignContent:"center",justifyItems:"center"}}>
            <Typography variant="h1" sx={{color:"white"}}>Тут будуть графіки</Typography>
        </Box>
    )
}